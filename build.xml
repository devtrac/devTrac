<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
              as the first entry and export the buildfile again. -->
<project basedir="." default="build" name="devTrac-Blackberry-46">
    <property environment="env"/>
    <property name="debuglevel" value="source,lines,vars"/>
    <property name="target.version" value="1.2"/>
    <property name="source.version" value="1.3"/>
	<property name="lib" value="${basedir}/lib" />
	<property name="classes" value="${basedir}/classes" />
	<property name="bin.main" value="${basedir}/bin/main" />
	<property name="bin.test" value="${basedir}/bin/test" />
	<property name="src.main" value="${basedir}/src/main" />
	<property name="src.test" value="${basedir}/src/test" />
	<property name="dir.src" value="${basedir}/src"/>
	<property name="dir.test" value="${basedir}/src/test"/>
	<property name="target" value="${basedir}/target"/>
	<property name="target.reports" value="${target}/reports"/>
	
    <path id="devTrac-Blackberry-46.classpath">
        <pathelement location="bin/main"/>
        <pathelement location="lib/net_rim_api.jar"/>
    </path>

	<path id="test.classpath">
          <pathelement location="bin/main"/>
          <pathelement location="bin/test"/>

	  <pathelement location="${target.product.classes}" />
	  <pathelement location="${target.test.classes}" />

	  <pathelement location="${src.test}/lib/junit.jar" />

	  <fileset dir="${lib}">
	    <include name="**/*.jar"/>
	  </fileset>
	</path>
	
    <target depends="cleanall" name="init">
        <mkdir dir="bin"/>
        <mkdir dir="${bin.main}"/>
        <mkdir dir="${bin.test}"/>
        <copy includeemptydirs="false" todir="bin">
            <fileset dir="src">
                <exclude name="**/*.launch"/>
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
    </target>
    <target name="clean">
        <delete dir="bin"/>
		<delete dir="${target}"/>
    </target>
    <target depends="clean" name="cleanall"/>
    <target depends="build-subprojects,build-main" name="build"/>
    <target name="build-subprojects"/>

    <target depends="init" name="build-main">
        <echo message="${ant.project.name}: ${ant.file}"/>
        <javac debug="true" debuglevel="${debuglevel}" destdir="${bin.main}" source="${source.version}" target="${target.version}">
            <src path="${src.main}"/>
            <classpath refid="devTrac-Blackberry-46.classpath"/>
        </javac>
    </target>
	
	<target name="compile.src">
		<mkdir dir="${target.product.classes}"/>
		<javac srcdir="${dir.src}" destdir="${target.product.classes}" >			
			<include name="*.java" />
		</javac>
	</target>
	
	<target name="compile.test">
		<javac debug="true" debuglevel="${debuglevel}" destdir="${bin.test}" source="${source.version}" target="${target.version}">
			<src path="${src.test}"/>
			<include name="**/*.java" />
			<classpath refid="test.classpath"/>
		</javac>
	</target>
	
	<target name="compile" depends="build-main, compile.test"/>

	<target name="UT" description="Run all JUnit tests." depends="clean, compile">
		<mkdir dir="${target.reports}"/>
		<junit fork="yes" haltonfailure="yes"> 
			<formatter type="plain" usefile="false" />
		    <classpath refid="test.classpath" />
			<batchtest todir="${target.reports}">
                <fileset dir="${dir.test}">
                    <include name="**/*Test.java"/>
                </fileset>
            </batchtest>
		</junit>
	</target>
    <target description="Build all projects which reference this project. Useful to propagate changes." name="build-refprojects"/>
</project>

